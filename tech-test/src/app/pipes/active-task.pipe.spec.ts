import { ActiveTaskPipe } from './active-task.pipe';

describe('ActiveTaskPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveTaskPipe();
    expect(pipe).toBeTruthy();
  });
});
